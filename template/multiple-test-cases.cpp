#include <bits/stdc++.h>

void solve_test_case() {}

int main() {
  std::ios_base::sync_with_stdio(0);
  std::cin.tie(0);
  std::cout.tie(0);
  int tests;
  std::cin >> tests;
  for (int test = 0; test < tests; test += 1) {
    solve_test_case();
  }
  return 0;
}